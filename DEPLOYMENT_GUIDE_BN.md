# 🚀 ফুল-স্ট্যাক প্রজেক্ট ডিপ্লয়মেন্ট ও ট্রাবলশুটিং গাইড

এই গাইডটিতে একটি MERN স্ট্যাক বা যেকোনো ফুল-স্ট্যাক ওয়েবসাইট (Frontend + Backend + Database) লাইভ সার্ভারে আপলোড করার পূর্ণাঙ্গ ধাপ এবং সাধারণত যেসকল সমস্যার সম্মুখীন হতে হয়, তার সমাধান দেওয়া হলো।

---

## ১. GitHub-এ কোড আপলোড করা

GitHub হলো আপনার কোড সেভ করে রাখার মূল জায়গা। Render বা Vercel এখান থেকেই কোড নিয়ে ওয়েবসাইট লাইভ করে।

**ধাপসমূহ:**
1. আপনার কম্পিউটারে VS Code-এর টার্মিনাল ওপেন করুন।
2. নতুন কোড সেভ করতে নিচের কমান্ডগুলো দিন:
   ```bash
   git add .
   git commit -m "আপনার পরিবর্তনের নাম বা মেসেজ"
   git push origin main
   ```
3. **Auto-Deploy:** Render ও Vercel-এ "Auto-Deploy" চালু থাকলে, GitHub-এ পুশ করার সাথে সাথেই আপনার ওয়েবসাইট অটোমেটিক আপডেট হয়ে যাবে।

---

## ২. MongoDB Atlas (ডাটাবেস) সেটআপ

আপনার ওয়েবসাইটের সব ডাটা এখানে সেভ থাকবে।

**ধাপসমূহ:**
1. MongoDB Atlas-এ প্রজেক্ট ও Cluster তৈরি করুন।
2. **Database User তৈরি:** 
   - বামপাশের মেনু থেকে **Database Access**-এ যান।
   - **Add New Database User**-এ ক্লিক করে ইউজারনেম ও পাসওয়ার্ড দিন।
   - *সতর্কতা:* পাসওয়ার্ডে `@`, `#`, `!` এর মতো স্পেশাল ক্যারেক্টার না দেওয়াই ভালো। যদি দেন, তবে Render-এ বসানোর সময় সেগুলো URL Encode করে দিতে হবে।
   - Role হিসেবে `Read and write to any database` সিলেক্ট করুন।
3. **Network Access (IP Whitelist):**
   - বামপাশের মেনু থেকে **Network Access**-এ যান।
   - **Add IP Address**-এ ক্লিক করুন।
   - **Allow Access from Anywhere** (`0.0.0.0/0`) সিলেক্ট করে Confirm করুন। এটি না করলে Render আপনার ডাটাবেসে ঢুকতে পারবে না।

---

## ৩. Render-এ Backend ডিপ্লয় করা

Node.js/Express ব্যাকএন্ড লাইভ করার জন্য Render খুব জনপ্রিয়।

**ধাপসমূহ:**
1. Render-এ গিয়ে **New > Web Service** সিলেক্ট করুন।
2. আপনার GitHub রিপোজিটরি কানেক্ট করুন।
3. Build Command: `npm install` (অথবা আপনার ফোল্ডার অনুযায়ী, যেমন `cd backend && npm install`)
4. Start Command: `node server.js` (বা `npm start`)
5. **Environment Variables (.env):**
   - Advanced-এ গিয়ে Environment Variables যোগ করুন।
   - `MONGO_URI` এর ভ্যালু হিসেবে MongoDB-র লিংকটি দিন।
   - লিংক ফরম্যাট: `mongodb+srv://<username>:<password>@cluster0...`

---

## ৪. Vercel-এ Frontend (React/Vite) ডিপ্লয় করা

Vercel ফ্রন্টএন্ড হোস্ট করার জন্য সবচেয়ে ভালো প্ল্যাটফর্ম।

**ধাপসমূহ:**
1. Vercel-এ গিয়ে **Add New > Project** সিলেক্ট করুন।
2. GitHub থেকে আপনার ফ্রন্টএন্ড ফোল্ডারটি ইম্পোর্ট করুন।
3. Framework Preset হিসেবে React বা Vite (আপনার যেটা) সিলেক্ট করুন।
4. **Environment Variables:** আপনার ফ্রন্টএন্ড যদি ব্যাকএন্ডের সাথে কানেক্ট করার জন্য কোনো API URL ব্যবহার করে (যেমন: `VITE_API_BASE_URL`), তবে সেটি এখানে বসিয়ে দিন। ভ্যালু হবে আপনার Render ব্যাকএন্ডের লাইভ লিংক।

---

## 🛑 সাধারণ সমস্যা ও সেগুলোর সমাধান (Troubleshooting)

ভবিষ্যতে ডিপ্লয় করার সময় নিচের সমস্যাগুলো হতে পারে। এখানে আপনার ফেস করা সমস্যাগুলো ও তার সমাধান লিখে রাখা হলো:

### সমস্যা ১: `MongoServerError: bad auth : authentication failed`
* **লক্ষণ:** সার্ভার ক্র্যাশ করে বা ডাটাবেসে কানেক্ট হয় না।
* **কারণ:** Render থেকে ডাটাবেসে ঢোকার সময় ইউজারনেম বা পাসওয়ার্ড ভুল হচ্ছে, অথবা ডাটাবেস Render-কে ব্লক করে রেখেছে।
* **সমাধান:**
  1. MongoDB Atlas-এর **Database Access**-এ গিয়ে চেক করুন ইউজারনেম ও পাসওয়ার্ড ঠিক আছে কিনা। প্রয়োজনে পাসওয়ার্ড রিসেট করুন।
  2. Render-এর **Environment Variables**-এ `MONGO_URI` ঠিকমতো দেওয়া আছে কিনা চেক করুন। 
  3. সবচেয়ে জরুরি: MongoDB Atlas-এর **Network Access**-এ `0.0.0.0/0` (Allow Access from Anywhere) অ্যাড করা আছে কিনা নিশ্চিত করুন।

### সমস্যা ২: `MongooseError: Operation buffering timed out after 10000ms`
* **লক্ষণ:** ওয়েবসাইট লোড হতে অনেক সময় নেয় এবং কোনো ডাটা দেখায় না। 
* **কারণ:** আপনার ব্যাকএন্ড ১০ সেকেন্ড ধরে ডাটাবেসের সাথে কানেক্ট হওয়ার চেষ্টা করে ব্যর্থ হয়েছে। এটি সাধারণত উপরের `bad auth` সমস্যার কারণে বা IP ব্লক থাকার কারণেই হয়।
* **সমাধান:** উপরের 'সমস্যা ১'-এর সমাধানটি ফলো করুন। কানেকশন ঠিক হলে এটি অটোমেটিক ঠিক হয়ে যাবে।

### সমস্যা ৩: কোড আপডেট করার পরও ওয়েবসাইটে চেঞ্জ না আসা
* **কারণ:** Render বা Vercel-এ Auto-Deploy বন্ধ থাকতে পারে, অথবা নতুন কোড বিল্ড হতে সময় নিচ্ছে।
* **সমাধান:**
  - ড্যাশবোর্ডে গিয়ে চেক করুন Auto-Deploy "Yes" করা আছে কিনা।
  - Logs বা Events ট্যাবে গিয়ে দেখুন বিল্ড প্রসেস শেষ হয়েছে কিনা।
  - প্রয়োজনে **Manual Deploy > Deploy latest commit**-এ ক্লিক করে নিজে থেকে রিস্টার্ট দিন।
